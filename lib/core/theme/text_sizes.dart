class TextSizes {
  // Extra Small text size (e.g., for captions, fine print)
  static const double XS = 10.0;

  // Small text size (e.g., for secondary labels, small details)
  static const double S = 12.0;

  // Medium text size (often used for body text, standard paragraphs)
  static const double M = 14.0;

  // Large text size (e.g., for prominent body text, subheadings)
  static const double L = 16.0;

  // Extra Large text size (e.g., for section titles, important headings)
  static const double XL = 20.0;

  // Double Extra Large text size (e.g., for main screen titles, hero text)
  static const double XXL = 24.0;

  // Triple Extra Large text size (e.g., for very large display text, splash screens)
  static const double XXXL = 32.0;

  // Quadruple Extra Large text size (e.g., for extremely large display text)
  static const double XXXXL = 48.0;
}
